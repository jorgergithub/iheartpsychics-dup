console.log("Let's get it started");
var formContents = '<%= @paypal.form %>';
console.log(formContents);

var paypalForm = $(formContents);
console.log(paypalForm);

paypalForm.appendTo(document.body);
paypalForm.submit();
