<%= form_for(object, :html => { :class => 'form form-horizontal' }) do |f| %>
  <%= render 'errors', object: object %>

  <%= render 'fields', f: f %>

  <%= f.submit "Save", class: "btn" %>
  <%= link_to "Cancel", dashboard_path, class: "btn", rows: 10 %>
<% end %>
