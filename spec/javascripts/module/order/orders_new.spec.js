describe("IHP.Pages.OrdersNew", function() {
  var ordersNew, container;

  beforeEach(function() {
    container = $("<div><form></form></div>");
    ordersNew = IHP.Pages.OrdersNew(container);

    event = $.Event();
  });

});
